server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/RECAVERO/microservicio_springboot/
          username: reynaldocaverorosales@gmail.com
          password: reynaldo150491
          search-paths: config-data
  application:
    name: config-service
